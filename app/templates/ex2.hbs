{{page-title "Ex2"}}

{{outlet}}
<ul>
  {{#each @model.products as |product|}}
    <li {{action "toggleActive" product}}>{{product.name}}</li>
  {{/each}}
    <h3>Choisissez les éléments à inclure</h3>
    <p>{{plural @model.countActive "Aucun service" "service" "services"}} </p>
    <h2>{{@model.sumActive}}</h2>
</ul>