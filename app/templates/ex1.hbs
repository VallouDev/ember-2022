{{page-title "ex1"}}
<h1>{{@model.nom}}</h1>
<div class="card-body">
  <Textarea class="form-control" @value={{this.content}}/>
</div>
<button type="button" id="save">Save</button>
<button type="button" id="delete" {{on "click" this.clear}}>Delete</button>
<button type="button" >clear</button>
<h1>{{this.content}}</h1>
<div class="je sais pas">
  <p>Nombres de caractères restant :  {{this.size}}</p>
</div>
{{outlet}}